import React, { FC } from 'react';

const SecretInfosAccessed: FC = () => {
  return <>{/* <SecretInfoAccessedList secretInfosAccessedResponse={data ?? []} /> */}</>;
};

export default SecretInfosAccessed;
